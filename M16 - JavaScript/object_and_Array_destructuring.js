const hena = {
    name:'hena',
    age:10,
    friends:['rafi','Sakib'],
    status:'not found'
}

const {name,age,friends,status} = hena;
console.log(name);

// Array Destructuring
const person = ["ALim", {name:"Sahriar"},"henani"];

const [person1,person2] = person;
console.log(person1, person2);

