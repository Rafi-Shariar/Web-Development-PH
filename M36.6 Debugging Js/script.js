for (let index = 0; index < 10; index++) {
    
    debugger; //works same as BreakPoint
    
    const element = index;
    console.log(element);
    
}

//To see debugger result -> inspect -> sources -> file_name
//But using breakpoint is best way. To see result follow the same process

 